# 共享服务端（规划中）

这里将存放社区共享 API。当前没有部署服务器，也没有任何扩展代码向远程服务发送数据。

实现前需要通过 Issue / Discussion 确定：

- Cloudflare Workers + D1、Supabase 或其他技术栈；
- AGPL-3.0 服务端许可证；
- 数据开放许可；
- 匿名身份、限流与日志保留策略；
- 候选片段成为可信片段的规则；
- 运维负责人和安全报告渠道。

接口草案见 [`docs/API.md`](../docs/API.md)。
