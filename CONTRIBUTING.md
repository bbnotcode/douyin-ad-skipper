# 参与贡献

感谢你帮助改进抖音网页版社区片段助手。项目目前处于早期实验阶段，抖音网页结构变化频繁，小而可验证的改动最容易合并。

## 开始之前

1. 搜索现有 Issue，避免重复工作。
2. Bug 请提供进入视频的入口（推荐、精选、搜索、用户主页或详情页）。
3. 请勿提交真实账号 Cookie、请求签名、访问令牌或未脱敏的个人数据。
4. 涉及共享服务器、数据结构或隐私的改动，建议先创建讨论 Issue。

## 本地开发

本项目目前不需要构建：

1. 克隆仓库。
2. 打开 `chrome://extensions/`。
3. 开启开发者模式并加载仓库根目录。
4. 修改文件后，在扩展页点击“重新加载”，再刷新抖音页面。

提交前至少运行：

```bash
node --check content.js
node --check popup.js
node --check options.js
python3 -m json.tool manifest.json >/dev/null
git diff --check
```

## 代码约定

- 保持原生 JavaScript/CSS，除非新依赖带来明确收益。
- DOM 选择器优先使用 `data-e2e`、语义属性和播放器标签，避免依赖随机 CSS 类名。
- 新的抖音页面适配必须保留旧入口的后备逻辑。
- 所有网络功能必须默认最小化数据，并在隐私说明中记录。
- 不得上传、嵌入或记录用户 Cookie、Authorization、设备指纹和完整浏览历史。

## Pull Request

PR 请说明：

- 解决的问题；
- 影响的抖音页面入口；
- 验证方法；
- 是否修改存储结构、权限或网络请求；
- 相关截图（如果是界面改动）。

提交贡献即表示你同意按照项目的 GPL-3.0 许可证发布你的贡献。
